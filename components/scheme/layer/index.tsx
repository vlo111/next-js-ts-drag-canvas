import { Layer } from "react-konva";
import React from "react";
import Nodes from './nodes'

const Scheme = () => {

    return (
        <Layer>
            <Nodes />
        </Layer>
    );
}

export default Scheme;
